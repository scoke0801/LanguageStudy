#include <string>
#include <vector>

using namespace std;

vector<int> solution(int n, long long k) {
    vector<int> answer;

    if (k < n)
    { 
    }

    while (k < n)
    {
        k -= (n - 1);
    }
    if ( n )
    return answer;
}
 